import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
});

export const metadata: Metadata = {
  title: "Solana AI IDE - Build Solana Programs with AI",
  description: "The ultimate AI-powered development environment for Solana blockchain. Build, deploy, and learn Solana programs with integrated AI assistance, wallet connection, and modern web IDE.",
  keywords: ["Solana", "AI", "IDE", "Blockchain", "Development", "Anchor", "Rust", "Web3"],
  authors: [{ name: "Solana AI IDE Team" }],
  viewport: "width=device-width, initial-scale=1",
  themeColor: [
    { media: "(prefers-color-scheme: light)", color: "white" },
    { media: "(prefers-color-scheme: dark)", color: "black" },
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        {children}
      </body>
    </html>
  );
}
